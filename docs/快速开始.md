# 快速开始指南

## 🚀 一键启动

### 1. 启动所有服务

```bash
# 方式一：使用 Makefile
make start

# 方式二：使用启动脚本
./start.sh
```

### 2. 访问系统

打开浏览器访问：**http://localhost:5174**

## 🔐 登录系统

### 默认账号（已预填）

登录页面已自动填充管理员账号，直接点击"登录"即可：

- **用户名：** admin
- **密码：** admin123

### 其他测试账号

如需切换账号，可以使用：

- **普通用户**
  - 用户名：demo
  - 密码：demo123

## 📝 功能测试

### 1. 聊天对话

登录后默认进入聊天界面，可以测试：

```
你好
Python课程适合什么人学习？
我的订单状态是什么？
帮我查询订单 ORD001
```

### 2. 知识库管理

点击左侧菜单"知识库"，可以：

- **批量导入知识**
  - 每行一条文本格式
  - JSON 格式（带元数据）

示例文本：
```
Python基础课程适合零基础学员，课程时长为40小时。
JavaScript进阶课程需要有JS基础，主要讲解ES6+新特性。
```

示例 JSON：
```json
{"text": "Python基础课程适合零基础学员", "metadata": {"course": "Python基础", "level": "初级"}}
{"text": "JavaScript进阶课程需要有JS基础", "metadata": {"course": "JavaScript进阶", "level": "中级"}}
```

### 3. 订单查询

点击左侧菜单"订单查询"，输入订单号：

```
ORD001
ORD002
ORD003
```

### 4. 租户管理

点击左侧菜单"租户管理"，可以：

- 查看当前租户
- 切换租户（default / t1 / t2）
- 管理租户配置

## 🛠️ 系统配置

### 查看系统状态

访问健康检查接口：
```bash
curl http://localhost:8000/health
```

### 查看可用模型

```bash
curl http://localhost:8000/models/list
```

### 切换 AI 模型

```bash
curl -X POST http://localhost:8000/models/switch \
  -H "Content-Type: application/json" \
  -d '{"name":"qwen-plus"}'
```

## 📊 API 测试

### 1. 聊天接口

```bash
curl -X POST http://localhost:8000/chat \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "query": "Python课程适合什么人？",
    "user_id": "test_user"
  }'
```

### 2. 知识库添加

```bash
curl -X POST http://localhost:8000/api/v1/vectors/items \
  -H "Content-Type: application/json" \
  -H "X-API-Key: test" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "items": [
      {
        "text": "Python基础课程适合零基础学员",
        "metadata": {"course": "Python基础", "level": "初级"}
      }
    ]
  }'
```

### 3. 订单查询

```bash
curl http://localhost:8000/api/orders/ORD001 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🔧 常见问题

### Q1: 登录按钮点击无反应？

**A:** 检查后端服务是否启动：
```bash
curl http://localhost:8000/health
```

如果无响应，重启后端：
```bash
./venv/bin/python work_v3/app.py
```

### Q2: 前端页面空白？

**A:** 检查前端服务是否启动：
```bash
# 查看进程
ps aux | grep "npm run dev"

# 重启前端
cd frontend && npm run dev
```

### Q3: 知识库查询无结果？

**A:** 检查 FAISS 索引是否存在：
```bash
ls -la work_v3/faiss_index/
ls -la work_v3/tenants/default/faiss_index/
```

如果不存在，重新构建索引：
```bash
cd work_v3 && ../venv/bin/python rag-train.py
```

### Q4: Token 过期怎么办？

**A:** Token 有效期为 7 天，过期后：
1. 自动跳转到登录页
2. 重新登录获取新 Token

### Q5: 如何添加新用户？

**A:** 在登录页面切换到"注册"标签，填写信息注册。

或通过 API：
```bash
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "newuser",
    "password": "password123",
    "nickname": "新用户",
    "email": "newuser@example.com"
  }'
```

## 📱 移动端访问

系统支持响应式设计，可以在移动设备上访问：

1. 确保移动设备和电脑在同一网络
2. 获取电脑 IP 地址：
   ```bash
   ifconfig | grep "inet "
   ```
3. 在移动设备浏览器访问：`http://YOUR_IP:5174`

## 🎨 界面特性

### Ant Design + Tailwind CSS

系统使用 Ant Design 和 Tailwind CSS 混合开发：

- **Ant Design**：复杂组件（Table, Form, Modal）
- **Tailwind CSS**：布局、间距、响应式

查看示例：
- 访问 `/examples/tailwind` 查看 Tailwind 示例
- 访问 `/examples/quickstart` 查看快速开始示例

## 📚 更多文档

- [登录功能说明](./登录功能说明.md)
- [查询示例](./查询示例.md)
- [Tailwind 使用指南](../frontend/TAILWIND_GUIDE.md)
- [API 规范](./api_specification.md)
- [架构设计](./architecture_design.md)

## 🆘 获取帮助

### 查看日志

```bash
# 后端日志
tail -f logs/backend.log

# 前端日志
tail -f logs/frontend.log

# 请求日志
tail -f work_v3/logs/requests.log
```

### 重启服务

```bash
# 停止所有服务
make stop
# 或
./stop.sh

# 启动所有服务
make start
# 或
./start.sh
```

### 清理缓存

```bash
# 清理 Python 缓存
make clean

# 清理前端缓存
cd frontend && rm -rf node_modules/.vite
```

## 🎯 下一步

1. ✅ 登录系统
2. ✅ 测试聊天功能
3. ✅ 导入知识库
4. ✅ 查询订单
5. 📝 自定义配置
6. 🚀 部署到生产环境

祝使用愉快！🎉
